/* Mock mapped netlist -- placeholder. In a real run this is the gate-level
   Verilog OpenSTA reads. The mock STA does not parse it; it exists so the
   pipeline's "netlist.v produced" check passes. */
/* Scripted scenario identity: @@LABEL@@ */
module nebula_top(clk_a, clk_b, rst_a_n, rst_b_n);
  input clk_a, clk_b, rst_a_n, rst_b_n;
  /* ... mapped cells omitted in mock ... */
endmodule
