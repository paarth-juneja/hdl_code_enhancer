No unconstrained endpoints found.

(All endpoints are constrained by clk_a, clk_b, clk_a_div, or clk_b_div. This
file existing and being empty of Startpoint blocks is what lets the parser
distinguish "no violations" from "nothing was checked".)
