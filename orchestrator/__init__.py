"""Nebula orchestrator.

Deterministic Python control of an EDA optimization loop in which a generative
model may propose bounded RTL patches and nothing else. See
``Nebula_file_workflow.md`` for the file-level dataflow this package implements.
"""

__version__ = "0.1.0"
SCHEMA_VERSION = "1.0.0"
